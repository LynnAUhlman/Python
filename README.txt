===STEPS===
--Note where the large .csv file is found.
--Update the split_csv.py file with the file path and how many lines per file needed.
--Open Anaconda
--Switch the file path to the split_csv.py directory: (example)
    cd /d "G:\My Drive\Python\Split_CSV"
--call the script:  \* switches the files to .xlsx format*/ 
    split_csv.py
--The script will note when the file has been split and is complete.
